name: Deploy to VM

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Oracle VM
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p ~/apps/server-config
            curl -so ~/apps/server-config/docker-compose.yml https://raw.githubusercontent.com/SoumojjalSen/server-config/main/docker-compose.yml
            curl -so ~/apps/server-config/Caddyfile https://raw.githubusercontent.com/SoumojjalSen/server-config/main/Caddyfile
            cd ~/apps/server-config
            docker-compose down --remove-orphans || true
            docker-compose pull
            docker-compose up -d
